# Resq backend – install these first (works on Python 3.14)
django>=5.0,<6.0
djangorestframework>=3.15,<4.0
django-cors-headers>=4.3,<5
requests>=2.32
python-dotenv>=1.0
waitress>=3.0,<4.0
psycopg[binary]>=3.2,<4.0

# Optional: for routing_optimization (DBSCAN, etc.) – uncomment if you use it from this repo
# numpy>=1.26
# scipy>=1.12
# scikit-learn>=1.4

# Optional: TensorFlow/PyTorch – not needed for Resq API (detectors run via subprocess).
# If you run the detector scripts in AI_Based_Disaster_Relief_And_Management_System, use Python 3.11/3.12 and:
# pip install tensorflow opencv-python
# pip install torch torchvision
