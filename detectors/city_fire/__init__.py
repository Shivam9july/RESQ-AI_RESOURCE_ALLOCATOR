"""
City Fire Detector integration.

This package is intended to wrap your existing `predict_fire.py` script and
`fire.model` model file into a reusable Python interface that can be called
from the Django backend.
"""

